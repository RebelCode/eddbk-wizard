import { commonMutations } from '@/store/mixins'

const state = {
  selected: null
}

const getters = {}

const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations: commonMutations
}
